platform :ios, '10.0'
use_frameworks!

# ignore all warnings from all pods
inhibit_all_warnings!

target 'NoriginTestTask' do
	
  pod 'Kingfisher', '~> 4.0'
  pod 'PromiseKit', '~> 6.0'
  pod 'TRON', '~> 4.0'

end
